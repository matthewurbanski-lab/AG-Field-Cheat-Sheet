export const metadata = {
  title: "Book Your Free Inspection | Matthew Urbanski — AquaGuard",
};

export default function BookLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
